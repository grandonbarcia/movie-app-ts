const getArticles = async () => {
  // const response = await fetch(
  //   `https://newsapi.org/v2/top-headlines?country=us&category=entertainment&apiKey=${NEXT_PUBLIC_}`
  // );
};

const ArticleList = () => {
  return <div>ArticleList</div>;
};

export default ArticleList;
